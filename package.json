{
  "name": "graphql-curso",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "transpile": "babel ./src --out-dir dist --copy-files",
    "build": "npm-run-all clean transpile",
    "server": "node ./dist/main.js",
    "prod": "set NODE_ENV=production && npm-run-all build",
    "dev": "set NODE_ENV=development && npm-run-all build server",
    "watch:dev": "nodemon",
    "start": "npm run prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "graphql-yoga": "^5.7.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.25.7",
    "@babel/core": "^7.25.8",
    "@babel/preset-env": "^7.25.8",
    "nodemon": "^3.1.7",
    "npm-run-all": "^4.1.5",
    "rimraf": "^6.0.1"
  }
}
